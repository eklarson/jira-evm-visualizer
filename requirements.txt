# Core FastAPI stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Prometheus metrics
prometheus-client>=0.21.0

# Data processing (for IMS XML + combining data)
pandas>=2.2.0

# Jira integration (reuse the existing wrapper if available)
# jira-integration-wrapper @ git+https://github.com/eklarson/jira-integration-wrapper.git

# Optional but recommended
python-multipart>=0.0.12
httpx>=0.27.0  # for async Jira calls if needed

# Development
ruff>=0.6.0
pytest>=8.3.0
pytest-asyncio>=0.24.0